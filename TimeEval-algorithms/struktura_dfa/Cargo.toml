[package]
name = "struktura-timeeval"
version = "0.1.0"
edition = "2021"
publish = false
description = "DFA anomaly detector for the ESA-ADB TimeEval benchmark"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
csv = "1"
libm = "0.2"
